Сложение без сложения(рекурсия)

def sum(a, b):
    a += 1
    b -= 1
    if b > 0:
        return sum(a, b)
    elif b == 0:
        return a
    else:
        return a - 1


a, b = int(input()), int(input())
print(sum(a, b))

